apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: infraindex-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: api.infraindex.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: infraindex-api-service
            port:
              number: 80

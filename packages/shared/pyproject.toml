[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "shared"
version = "0.1.0"
dependencies = [
    "sqlalchemy",
    "pydantic",
    "pydantic-settings"
]
